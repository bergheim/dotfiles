-- yay settings. Overlays config.json; CLI flags still win over both.

-- Replaces the old `--devel --timeupdate`; --timeupdate was removed in yay 13.
yay.opt.devel = true
yay.opt.answer_diff = "All"
yay.opt.remove_make = "yes"
