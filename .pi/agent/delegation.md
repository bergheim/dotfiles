When a `worker` subagent is available, delegate trivial, fully-specified edits and searches to it -- it runs a cheap local model. Use `codex` for complex implementation, refactors, and debugging where GPT-5.6's coding specialization fits best. Use `claude` for long-horizon implementation, independent alternate approaches, and code review where a different frontier model is valuable. Both frontier implementation agents run at medium effort; reserve xhigh reasoning for explicit advisor escalation. Keep design, judgment, and orchestration on the primary model.
