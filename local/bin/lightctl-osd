#!/bin/sh
# lightctl + avizo -d when the session is dark (light-colored icons).
extra=
[ "$(gsettings get org.gnome.desktop.interface color-scheme)" = "'prefer-dark'" ] && extra=-d
exec lightctl $extra "$@"
